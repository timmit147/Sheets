The main javascript file looks to the json fil find the name of a block and than searches in the blocks folder for that name. When that block is found the html is placed on the page css and javascript added. if the css and javascript already exsist than only add the html.